<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quanta | <%= title %></title>
  <link rel="stylesheet" href="style.css">
</head>

<body>
  <h1>Search</h1>
  <form method="GET">
    <div class="search-field">
      <label class="search-icon-container" for="search">🔍</label>
      <input type="text" name="keyword" placeholder="Search" id="search">
    </div>
  </form>
  <% if(items.length > 0) {%>
  <%- include('partials/itemCardContainer', {items: items}) %>
  <% } else if (keyword) {%>
  <div class="no-items-found-message">No item '<%= keyword %>' found</div>
  <% } else {%>
  <div class="search-default-message">
    <div class="icon-container">
    </div>
    <h2>Nothing to see here</h2>
    <h3>Enter text to start your search</h3>
  </div>
  <% }%>

  <%- include('partials/nav') %>
</body>

</html>